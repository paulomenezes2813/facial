// Substituído por EventoDialog (suporta create + edit). Mantido aqui apenas como
// re-export para compatibilidade com qualquer importação antiga remanescente.
export { EventoDialog as CriarEventoDialog } from './EventoDialog';
