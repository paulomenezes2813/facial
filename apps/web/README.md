# Web

Next.js 14 (App Router) servindo:
- `/credenciamento` — fluxo público de pré-cadastro (mobile)
- `/admin` — painel administrativo
- `/totem` — modo kiosk para o check-in no evento

## Setup

```bash
pnpm install
pnpm --filter @facial/web dev
```

Sobe em http://localhost:3000.
