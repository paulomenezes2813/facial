{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/api/Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'cd /repo/apps/api && (pnpm prisma migrate deploy 2>&1 || echo MIGRATE_FAILED) && (node prisma/seed.js 2>&1 || echo SEED_FAILED) && node dist/src/main.js 2>&1'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30
  }
}
